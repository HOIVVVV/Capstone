데이터셋 구조
    라벨링데이터터 : 라벨별로 각 이미지 데이터에 대한 정보가 annotations.json 파일로 존재
    원천데이터 : 라벨별로 이미지 데이터 존재
    라벨: 손상(1)/비손상(2)-손상유형(num)-동일 손상 유형, 다른 형태(num)
        예시 1-1-1

DatasetMake.py 데이터셋 이미지 파일메 맞춰, annotations.json 파일을 수정하는 코드
mLearn.py resNet50xt 모델로 이미지 학습 코드
app.py 앱 실행 파일
main.py app.py 백엔드 구현 파일
routes/main.py route 정의 및 구현 파일
FrontEnd Angualar 프론트엔드 파일 

###라이브러리 설치###
1.pip install -r requirements.txt

#pytorch CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#pytorch cpu
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

###Angular 실행법###
1.node.js 설치 (cmd에 node -v 쳐서 버전 안뜨면 설치)
2.VScode 실행, cd FrontEnd 쳐서 Angualr 루트로 이동, cmd 터미널에 npm install
3.이후 npm install -g @angular/cli 
4.ng serve로 실행
5.http://localhost:4200 로 접속


###노트북###
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

